name: PR Title Manager
# [AUTOMATED] Normalises the PR title to Conventional Commits so the squash-merge
# commit (PR title + concatenated commits) drives versioning/changelog cleanly.

on:
  pull_request:
    # `edited` so a title changed AFTER the first run is re-validated (else a
    # stale green check could merge a non-conventional title). The bot-actor
    # guard below stops this job's own title edit from looping.
    types: [opened, reopened, synchronize, edited]

concurrency:
  group: pr-title-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  manage-title:
    if: github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      issues: write
      contents: read
    steps:
      - name: Classify the PR (bot? fork?)
        id: ctx
        env:
          ACTOR: ${{ github.actor }} # via env, never inline in run: (injection)
          HEAD_REPO: ${{ github.event.pull_request.head.repo.full_name }}
          BASE_REPO: ${{ github.event.pull_request.base.repo.full_name }}
        run: |
          # Skip bot-authored PRs (e.g. dependabot) — titles already conventional.
          [[ "$ACTOR" == *"[bot]"* ]] && echo "skip=true" >> "$GITHUB_OUTPUT" || echo "skip=false" >> "$GITHUB_OUTPUT"
          # Fork PRs run with a read-only token AND their checkout is attacker-
          # controlled — so we must NOT check out / execute their script.
          [[ "$HEAD_REPO" != "$BASE_REPO" ]] && echo "fork=true" >> "$GITHUB_OUTPUT" || echo "fork=false" >> "$GITHUB_OUTPUT"

      # SAME-REPO ONLY: it is safe to check out and run the (PR-modifiable) script.
      - name: Checkout the shared script
        if: steps.ctx.outputs.skip == 'false' && steps.ctx.outputs.fork == 'false'
        uses: actions/checkout@v7
        with:
          sparse-checkout: |
            .github/scripts

      - name: Normalise PR title (same-repo)
        if: steps.ctx.outputs.skip == 'false' && steps.ctx.outputs.fork == 'false'
        uses: actions/github-script@v9
        with:
          script: |
            const { processPRTitle } = require(
              `${process.env.GITHUB_WORKSPACE}/.github/scripts/normalize-pr-title.js`
            );
            const pr = context.payload.pull_request;
            const { owner, repo } = context.repo;
            const issue_number = pr.number;

            // Opt-out: leave the title alone (but warn — semantic tooling depends on it).
            if (pr.title.includes('[skip title auto pr]')) {
              core.info('Opt-out marker present; not touching the title.');
              return;
            }

            const commits = await github.paginate(github.rest.pulls.listCommits, {
              owner, repo, pull_number: issue_number, per_page: 100,
            });
            const { newTitle, changed } = processPRTitle(pr.title, commits, pr.head.ref);

            if (changed && newTitle !== pr.title) {
              await github.rest.pulls.update({ owner, repo, pull_number: issue_number, title: newTitle });
              core.info(`PR title → ${newTitle}`);
            }

            // Sticky comment explaining the convention (upserted).
            const marker = '<!-- pr-title-manager -->';
            const body = `${marker}
            ## 🤖 PR title (Conventional Commits)

            **Title:** \`${newTitle}\`

            Because we **squash-merge**, this title becomes the commit message on \`main\`
            (the individual commits become its body), so it must be a Conventional Commit —
            that's what drives automated versioning and the changelog.

            Opt out with \`[skip title auto pr]\` (not recommended).`;
            const comments = await github.paginate(github.rest.issues.listComments, {
              owner, repo, issue_number, per_page: 100,
            });
            const existing = comments.find((c) => c.body?.includes(marker));
            if (existing) await github.rest.issues.updateComment({ owner, repo, comment_id: existing.id, body });
            else await github.rest.issues.createComment({ owner, repo, issue_number, body });

      # FORK PRs: can't run their script (untrusted) or write (read-only token).
      # Validate the title INLINE (no checkout, no fork code) and FAIL if it isn't
      # a Conventional Commit — so a fork can't merge a non-releasing title.
      - name: Validate fork PR title (read-only, no fork code)
        if: steps.ctx.outputs.skip == 'false' && steps.ctx.outputs.fork == 'true'
        uses: actions/github-script@v9
        with:
          script: |
            const title = context.payload.pull_request.title;
            const TYPES = ['feat','fix','docs','style','refactor','perf','test','build','ci','chore','revert'];
            const conventional = new RegExp(`^(${TYPES.join('|')})(\\([^)]+\\))?!?: .+`);
            if (!conventional.test(title)) {
              core.setFailed(
                `Fork PR title must be a Conventional Commit (it becomes the squash commit): "${title}"`
              );
            }
